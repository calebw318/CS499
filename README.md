# CS499
CS 499 project

1. Update .env file! Make sure there is a file called .env (rename the .env_example in this repository) and configure with the Supabase URL and secret key for the project.
2. Ensure Flask, Supabase, python-dotenv installed on your environment
3. Run app.py on a server. Use the provided IP to access the website. In the future, will be running continuously and have an associated URL.
